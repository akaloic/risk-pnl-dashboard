# Requires Python 3.11-3.14. Every pin below ships a wheel for all four, so a
# first install never falls back to compiling Rust or C. pandas 3.0 is the
# floor that sets the range: it is the only branch with 3.14 wheels, and it
# needs 3.11 or newer itself.
fastapi==0.141.1
uvicorn[standard]==0.52.3
pandas==3.0.5
pydantic==2.13.4
python-dateutil==2.9.0.post0
pytest==9.1.1
httpx==0.28.1
